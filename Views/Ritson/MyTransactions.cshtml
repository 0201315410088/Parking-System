@model List<RitsonProfileTransactionViewModel>

@{
    ViewData["Title"] = "My Transaction History";
    Layout = "_Layout";
}

<h2>My Transaction History</h2>

<table class="table">
    <thead>
        <tr>
            <th>Booking ID</th>
            <th>User Name</th>
            <th>Start Time</th>
            <th>End Time</th>
            <th>Cost</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var transaction in Model)
        {
            <tr>
                <td>@transaction.RitsonTransactionHistory.BookingId</td>
                <td>@transaction.Profile.FirstName</td> <!-- Access Username through Profile -->
                <td>@transaction.Profile.LastName</td> <!-- Access Username through Profile -->
                <td>@transaction.Profile.Username</td> <!-- Access Username through Profile -->
                <td>@transaction.Profile.UserType</td> <!-- Access Username through Profile -->
                <td>@transaction.RitsonTransactionHistory.StartTime</td>
                <td>@transaction.RitsonTransactionHistory.EndTime</td>
                <td>@transaction.RitsonTransactionHistory.Cost</td>
            </tr>
        }
    </tbody>
</table>

@* @model IEnumerable<testsubject.Models.Booking>

<h2>Payments</h2>
<a href="@Url.Action("Index", "Home")">View Payments</a>

<table class="table">
    <thead>
        <tr>
            <th>Booking ID</th>
            <th>Customer Name</th>
            <th>Cost</th>
            <th>Start Date</th>
            <th>End Date</th>
            <th>Status</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var booking in Model)
        {
            <tr>
                <td>@booking.BookingId</td>
                <td>@booking.CarNumberPlate</td>
                <td>@booking.Cost.ToString("C")</td>
                <td>@booking.StartDate.ToShortDateString()</td>
                <td>@booking.EndDate.ToShortDateString()</td>
                <td>@booking.BookingStatus</td>
            </tr>
        }
    </tbody>
</table>
 *@